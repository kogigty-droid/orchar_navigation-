## 思路 架构：
```
本研究面向果园空地协同作业场景，构建无人机任务引导下的地面车辆自主运动规划方法。无人机作为空中感知与任务发布端，获取目标作业区域或接驳点位置信息，
并通过通信链路向地面车辆发送任务信号；地面车辆作为执行端，融合自身 RTK 定位、果园行间环境信息和无人机下发目标点，完成目标点坐标转换、可达性判断、全局路径规划、局部避障与路径跟踪控制。
针对果园环境中树行遮挡、道路狭窄、定位误差和动态障碍物等问题，设计任务状态驱动的路径重规划机制，实现地面车辆对无人机任务的快速响应和自主接近。

```

专利相关思路：
借鉴ALNS，目标点不是直接给定，而是在候选点集合中，结合车辆、无人机、环境和时序约束进行优化选择。

```
S1：无人机获取任务点或当前位置，并发送给小车；

S2：小车将无人机位置转换到局部 ENU 坐标系；

S3：根据果园行间道路、树行边界和障碍物地图，在无人机投影点附近生成候选接驳点集合；

S4：剔除不可通行、不可停车、通信距离过远、与障碍物距离过近的候选点；

S5：对剩余候选点建立综合代价函数，评价小车行驶距离、无人机偏移距离、路径风险、定位置信度和等待时间；

S6：选择总代价最小的候选点作为小车目标点；

S7：基于该目标点进行全局路径规划和局部避障；

S8：到达目标点附近后，利用视觉标志、UWB 或 AprilTag 进行末端对准。
```
